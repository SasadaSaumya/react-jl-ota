<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!--
    The OTA protocol runs natively but the BLE transport is driven by the host
    app (react-native-ble-plx). These permissions let the SDK resolve a
    BluetoothDevice from its MAC address. The host app is expected to already
    request the runtime BLE permissions for scanning/connecting.
  -->
  <uses-permission android:name="android.permission.BLUETOOTH" />
  <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
  <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
  <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
  <!-- Required for the optional remote-URL firmware download. -->
  <uses-permission android:name="android.permission.INTERNET" />

</manifest>
